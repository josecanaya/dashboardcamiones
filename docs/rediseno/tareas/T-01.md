# T-01 · Normalizar tipografía del selector de procesos guardados

- **Tipo**: mecánica · **Ejecuta**: agente barato · **Costo estimado**: ~4 k tokens
- **Depende de**: ninguna

## Leer (solo esto)
- `src/features/real-truckflow/components/SavedWindowsPicker.tsx`

## Editar
- `src/features/real-truckflow/components/SavedWindowsPicker.tsx`

## Hacer
1. Sustituir todos los literales `text-[11px]` por `text-xs`.
2. Sustituir todos los literales `text-[10px]` por `text-xs`.
3. No cambiar ningún otro token de clase ni texto.

## No hacer
- No alterar agrupación, orden, períodos, estados stale, disabled, onPick ni activeRunId.
- No extender el reemplazo a otros archivos.

## Verificar
`npm test && npm run check:arch`. `rg -n 'text-\[(10|11)px\]' src/features/real-truckflow/components/SavedWindowsPicker.tsx` no devuelve coincidencias. En Análisis local los procesos guardados conservan sus botones, rangos y estados; sin texto cortado a 1440×950.

## Si algo no coincide
Parar y reportar. No improvisar.
