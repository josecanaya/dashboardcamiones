# T-02 · Normalizar tipografía de las tarjetas de cámara

- **Tipo**: mecánica · **Ejecuta**: agente barato · **Costo estimado**: ~4 k tokens
- **Depende de**: ninguna; puede convivir con T-03, pero ejecutar commits en serie por compartir archivo

## Leer (solo esto)
- `src/components/plant/LiveCameraPlayerModal.tsx`

## Editar
- `src/components/plant/LiveCameraPlayerModal.tsx`

## Hacer
1. Sustituir `text-[9px]`, `text-[10px]` y `text-[11px]` por `text-xs` en este archivo.
2. Mantener sin cambios el resto de cada className.

## No hacer
- No tocar URLs, APIs, streams, gridClassFor, credenciales, iframe, estados ni efectos.
- No cambiar colores: el video conserva fondo oscuro.

## Verificar
`npm test && npm run check:arch`. `rg -n 'text-\[(9|10|11)px\]' src/components/plant/LiveCameraPlayerModal.tsx` no devuelve coincidencias. Abrir desde `/` un grupo de cámaras: etiquetas y botón Cerrar legibles, sin solapamientos; cerrar desmonta los reproductores.

## Si algo no coincide
Parar y reportar. No improvisar.
