# R09 · Terminar la retirada de Transform como pantalla

- **Tipo**: mecánica con conservación de resultados · **Ejecuta**: agente económico · **Costo estimado**: ~3 k tokens
- **Depende de**: R08

## Leer (solo esto)
- `src/features/real-truckflow/tabs/TransformEtlTab.tsx`.
- `src/App.tsx` y resultados de `rg -n 'TransformEtlTab' src`.
- `docs/rediseno/INVENTARIO.md`: inventario de Transform.

## Editar
- Renombrar `TransformEtlTab.tsx` a `ExecutiveSummaryTab.tsx` en el mismo directorio.
- Cambiar export `TransformEtlTab` a `ExecutiveSummaryTab` y las referencias identificadas de ese símbolo.
- Componente de resumen y sus mensajes visibles solamente.

## Hacer
1. Renombrar archivo/export/imports. No mover helpers a otro módulo ni cambiar cálculos.
2. La única ruta de la pantalla es `/estadisticas/reportes/resumen`. URLs viejas siguen siendo redirects.
3. Eliminar de este informe el bloque «Fuentes Excel y backup» que envuelve MovimientosBackupPanel: ya está disponible en Datos tras R07. Quitar solo ese import si queda sin usos.
4. Estado vacío del informe: explicación corta + Link «Preparar datos» a Datos. El picker de procesos no se duplica en el informe: está en Datos/Avanzado y barra común.
5. Mantener filtros de producto/muestra, donuts, comparativas, conciliación, anomalías/sospechas, San Lorenzo, cobertura, drilldowns, todos los botones de CSV y diagnóstico DEV. «Ejecutando transform…» visible cambia a «Preparando datos…».
6. No renombrar funciones/tipos que incluyan Transform en el motor. El pedido elimina la pantalla técnica y su paso manual normal, no el procesamiento.

## No hacer
- No borrar datos/exportaciones ni suponer que la pantalla solo era un botón de ETL.
- No ejecutar limpieza general de los 16 archivos antiguos candidatos.

## Verificar
`rg -n 'TransformEtlTab' src` sin resultados; nav sin Transform. `npm test -- --maxWorkers=2`, `npm run check:arch`. Comparar inventario/exportes antes/después usando misma corrida guardada; mismos datos y archivos, distinto acceso.

## Si algo no coincide
Parar y reportar. No improvisar.
