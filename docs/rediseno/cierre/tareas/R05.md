# R05 · Conectar preparación y resultado al mismo contexto

- **Tipo**: implementación especificada · **Ejecuta**: agente económico · **Costo estimado**: ~6 k tokens
- **Depende de**: R04

## Leer (solo esto)
- `docs/rediseno/cierre/CONTRATO.md`, sección 1.
- `etlWorkbench/EtlWorkbenchContext.tsx`: Ctx, resetLoadedState, clearLoaded, activateHistoricalPeriod, paths de carga, valor/dependencias del provider.
- `dataPreparation/useDataPreparation.ts` y types.

## Editar
- `src/features/real-truckflow/etlWorkbench/EtlWorkbenchContext.tsx`.
- Tests de integración de preparación con adaptador de contexto en dataPreparation.

## Hacer
1. Instanciar useDataPreparation una vez en provider. Exponer objeto `dataPreparation` con state, setDraft, inspect, execute, requestStop y retry. No mover provider ni crear contexto paralelo de tablas.
2. Extraer la publicación de activateHistoricalPeriod a callback común: aplica resetLoadedState(false) o equivalente actual, fechas/output/cache/composed/KPI coherentes. Usarlo en activate y publish del nuevo runner; no llamar clearLoaded que invalida revisión en pleno commit.
3. Active del nuevo estado es el período de las tablas, nunca el borrador/extracción. Las APIs legacy conservadas deben notificar éxito al mismo adaptador, con limitations si stale/parcial. Eventos crudos tardíos no pueden contaminar un rango posterior.
4. `isDataMutationBusy` agrega cola nueva, busyLoad, transformBusy, kpiTiemposBusy y cargas manuales guardadas. Guardar por ref cada operación; no confiar solo en disabled visual. Limpiar y cargas manuales retornan sin mutar si hay operación incompatible.
5. Reemplazar auto-hidratación duplicada por una lectura inicial única del último rango persistido: puede abrir caché vigente, nunca descargar/reprocesar al montar. Falla conserva estado explícito para reintento.
6. Tests: A activo/B falla conserva A; éxito B limpia eventos A; respuesta de A tardía descartada; limpieza durante proceso no muta; contexto permanece al cambiar ruta; errors liberan lock. No alterar firmas legacy utilizadas por herramientas avanzadas.

## No hacer
- No cambiar buildKpiTiemposArtifacts, createTransformPhaseSession, cálculo de tramos o tablas.

## Verificar
Tests nuevos, `npm test -- --maxWorkers=2`, `npm run check:arch`; TypeScript sin diagnósticos nuevos respecto de baseline medido.

## Si algo no coincide
Parar y reportar. No improvisar.
