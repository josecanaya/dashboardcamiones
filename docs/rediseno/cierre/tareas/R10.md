# R10 · Unificar presentación sin cambiar semánticas

- **Tipo**: mecánica con reglas cerradas · **Ejecuta**: agente económico · **Costo estimado**: ~5 k tokens
- **Depende de**: R09

## Leer (solo esto)
- `docs/rediseno/cierre/CONTRATO.md`, sección 6.
- `src/components/ui/ui.css`, `src/app/appShell.css`, `src/pages/PlantHome.tsx`, `src/pages/plantHome.css`.
- Tabs ExecutiveSummary, KpiTiempos, Calada, CaladaCamerasPanel, Descargas y Seguridad.

## Editar
- Solo los archivos leídos. En Seguridad respetar cambios concurrentes del usuario.

## Hacer
1. En className estáticos de texto de esos tabs, sustituir `text-[9px]`, `[9.5px]`, `[10px]`, `[10.5px]`, `[11px]`, `[11.5px]` (cada uno con prefijo text-) por text-xs; `text-[12.5px]`, `text-[13px]` por text-sm. No tocar fontSize en props SVG/Recharts, coordenadas o estilos de imágenes.
2. Colores de interacción Tailwind `violet-*` en className de botones/input/nav → `blue-*` con mismo sufijo numérico/opacidad. No sustituir violet en etiquetas de circuito, chips de anomalía, barras/gráficos o constantes de datos. Si el elemento mezcla interacción con semántica, excluirlo y listar la exclusión; no elegir otro color.
3. Eliminar duplicación de título de ruta en los tabs que ya lo reciben de HistoricalWorkspace: conservar títulos de secciones; retirar únicamente el nodo de encabezado principal si su texto normalizado coincide exactamente con el título de HistoricalWorkspace; conservar cualquier descripción y cualquier encabezado con texto distinto. Mapeo exacto: Calada/Descargas conservan su selector de sede; Seguridad conserva header de detalle/camión pero no repite título de listado; Executive conserva «Circuitos y cobertura» como sección.
4. Home: mantener tres vistas actuales. Cambiar valores de ausencia visibles `—` a «sin dato» únicamente donde la expresión ya usa null/undefined; no tocar ceros reales, backlog, tasas o denominadores. No cambiar la fórmula de ningún valor.
5. En CSS Home reemplazar colores #475569/#1d4ed8 de reglas de texto/foco por var(--ui-muted)/var(--ui-primary); reutilizar tf-ui. No selectores nuevos que alcancen iframes/video/mapa.
6. Verificar ancho de shell: a 1024px no superponer sidebar/contenido; si sidebar fuerza overflow, reducir ancho a 208px solo entre 768–1100px. No cambiar rutas ni ocultar enlaces. Tablas conservan scroll horizontal propio.

## No hacer
- No redibujar plano ni cambiar colores de estado/circuito; no editar JSON de posiciones ni cámaras.
- No convertir indiscriminadamente todos los hex o todas las palabras violet.

## Verificar
`npm test -- --maxWorkers=2`, `npm run check:arch`; capturas reales a 1440×950 y 1024×768 de Home, Datos, KPI, Calada, Descargas, Anomalías y Resumen. Contraste de texto normal ≥4.5:1 y foco visible. No capturar credenciales/URLs de streams.

## Si algo no coincide
Parar y reportar. No improvisar.

