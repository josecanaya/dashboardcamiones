# R04 · Ejecutar la cola con reintento seguro

- **Tipo**: implementación especificada · **Ejecuta**: agente económico · **Costo estimado**: ~8 k tokens
- **Depende de**: R03

## Leer (solo esto)
- `docs/rediseno/cierre/CONTRATO.md`.
- Archivos de R03.
- `api/truckflowLocalServerApi.ts`, `api/movimientosBackupApi.ts`, `api/etlRunCacheApi.ts`.
- `etlWorkbench/historicalPeriodLoader.ts`.

## Editar
- Crear `dataPreparation/preparationRunner.ts`, `preparationRunner.test.ts` y `useDataPreparation.ts`.
- `etlWorkbench/historicalPeriodLoader.ts`: extraer/exportar su función de inspección sin cambiar cálculos ni carga; reutilizarla desde loader y runner.

## Hacer
1. Runner con dependencias inyectables (APIs/dispatch/publish), sin React. `inspect(range)` solo lee y devuelve plan. `execute(plan, options)` usa orden del contrato. `requestStop()` solo marca detener después de llamada activa.
2. Hook usa reducer y ref lock. Impedir doble click antes del await. Todos los eventos llevan operationId. Settle libera lock incluso si falla red.
3. Descargar días pendientes con postTruckflowExportOneDay/site all; status error es fallo aunque HTTP sea 200. Detener cola, preservar éxitos, no procesar si faltan fuentes requeridas.
4. Procesar con requestRunEtl únicamente las ventanas que resolveWindow confirme ausentes/stale; force true solo stale. Reresolver después. Si corrida recién procesada sigue stale/no existe, failed; no publicar.
5. Último paso llama loadHistoricalPeriod(range,{processMissing:false}); solo output completo habilita publish. No llamar a clearLoaded, loadLocalPeriod o runTransform desde runner.
6. Implementar persistencia/reanudación y límites de hoy/futuro exactamente como contrato. Reintentar siempre vuelve a inspect y no repite descargas available ni runs vigentes.
7. Tests con mocks y promesas diferidas: orden de llamadas, clic doble, navegación no cancela, stop tras paso activo, error HTTP/200-error, reload interrupted, Excel incompleto sin opt-in, tablas página 2 fallida y no-publicación, stale force true, cache vigente cero POST.

## No hacer
- No ejecutar extracción real en tests, no reinventar runner ETL ni cancelar un proceso del servidor al abortar fetch.
- No iniciar operaciones de escritura al montar.

## Verificar
Tests nuevos pasan, `npm test -- --maxWorkers=2`, `npm run check:arch`. Con mocks la publicación ocurre exactamente una vez tras el último paso.

## Si algo no coincide
Parar y reportar. No improvisar.
