# R11 · Validar, documentar y cerrar entrega

- **Tipo**: verificación especificada · **Ejecuta**: agente económico · **Costo estimado**: ~5 k tokens
- **Depende de**: R10

## Leer (solo esto)
- `docs/rediseno/cierre/EMPEZAR_AQUI.md`, CONTRATO.md, ESTADO.md.
- `docs/rediseno/INVENTARIO.md`, INFORME.md y logs de baseline guardados al empezar.
- Cambios de R01–R10 (`git show` por commit).

## Editar
- `docs/rediseno/cierre/ESTADO.md`, crear RESULTADO.md y carpeta evidencia/.
- Reparaciones solo en archivos introducidos/modificados por R01–R10; una corrección por commit con motivo. No abrir alcance ETL.

## Hacer
1. Ejecutar `npm test -- --maxWorkers=2`, `npm run check:arch`, `npx tsc -b --pretty false`. Guardar exit codes reales, logs y commit. En PowerShell no usar éxito del último Get-Content como éxito de npm. Si npm pierde argumentos, usar `node node_modules/vitest/vitest.mjs run --maxWorkers=2` para contraste y registrar ambas invocaciones.
2. Comparar TypeScript por archivo+código+mensaje (ignorar solo número de línea desplazada). Baseline histórico 191 no es permiso para introducir un error y borrar otro. Ningún error nuevo aceptado.
3. Baseline semántico conocido: etlCaladaCameraActivity 2, etlSanLorenzoVolcableActivity 3, etlRicSanLorenzoRoute 1, etlSegmentTiming 1. No cambiar esos cálculos/tests. Todo fallo adicional requiere reparación o bloqueo explícito; no declarar suite verde si persisten fallos.
4. Casos de UI obligatorios: caché vigente abre con cero POST; falta un día descarga lo necesario; archivo error no disponible; unknown no se confunde con cero; Excel incompleto requiere elección explícita y limita etiqueta; error de red conserva A al intentar B; reintento no repite éxitos; doble click solo una cola; navegación conserva proceso; recarga muestra interrupted; hoy requiere ajustar hasta ayer; mes cruza semanas sin ventanas ad hoc; tabla >10000 filas íntegra.
5. Regresión: tres vistas Home, circuito, zona/sector, cámaras DSS de grupo y cierre, NVAi, editor del plano; filtros/CSV Calada/Descargas/KPI; búsqueda/páginas y ocultar/restaurar Anomalías; cinco reportes; exportaciones del resumen; cada herramienta avanzada del inventario. Para escrituras usar mocks, no reprocesar datos reales sin necesidad.
6. Capturas reales con corrida guardada identificada (runId y período), antes/después si baseline existe. Si no hay captura anterior, declararlo; no recrearla con datos inventados. Simulaciones de errores/carga se rotulan como pruebas.
7. Actualizar inventario con ubicación nueva de cada función, no solo marca «preservado». En RESULTADO incluir commits, cambios, pruebas, limitaciones, pendientes con dueño, y pasos para revertir cada unidad con git revert.
8. Verificar que stage/commit no incluye cambios de otro agente ni archivos de credenciales. Copiar paquete final a outputs de la conversación y dar al usuario enlace a RESULTADO/ESTADO.

## No hacer
- No cerrar aceptación global con tests fallidos sin indicar causa y alcance.
- No reemplazar pruebas interactivas por contar elementos del código.

## Verificar
Cada R01–R10 tiene commit, resultado de pruebas e inventario de funciones actualizado. La UI ya no pide pasar por Extracción → Análisis → Transform para el caso normal. Lo que no pueda verificarse queda explícito, con evidencia y siguiente paso.

## Si algo no coincide
Parar y reportar. No improvisar.
