# La capa de agentes corre en la SUSCRIPCIÓN de Claude Code (sin API key).
# NO seteés ANTHROPIC_API_KEY: el modelo lo provee Claude Code, no la API.

# Base del ETL API que el servidor MCP consulta.
ETL_API_BASE=http://127.0.0.1:8787
